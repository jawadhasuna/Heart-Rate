# The MIT-BIH data — 100+ MB, downloaded from PhysioNet, not ours to redistribute.
data/

# Python
.venv/
__pycache__/
*.pyc

# Local editor / tooling settings
.claude/

# Trained model and built dataset (regenerate with build_dataset.py / train.py)
beats.npz
model.pt
record_*.png
